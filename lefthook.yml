pre-commit:
  jobs:
    - name: whitespace
      run: git diff --cached --check

pre-merge-commit:
  jobs:
    - name: whitespace
      run: git diff --cached --check

pre-push:
  jobs:
    - name: research checks
      run: pnpm run check
